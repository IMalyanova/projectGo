package main

func main() {
	//for
	for init; condition; post{}

	//while
	for condition{}

	//for ; ;
	for{}
	//================================
	summ: = 0
	for i: = 0; i < 10; i ++ {
		sum + = i
	}
	//	============================
	for key, value := range oldMap {
		newMap[key] = value
	}
	//=================================
	for key := range m {
		if key.expired(){
			delete(m, key)
		}
	}
	//	=========================

}
